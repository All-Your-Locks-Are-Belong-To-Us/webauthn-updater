{
  "web": {
    "client_id": "${WAU_KEYCLOAK_CLIENT_ID}",
    "client_secret": "${WAU_KEYCLOAK_CLIENT_SECRET}",
    "auth_uri": "https://${WAU_KEYCLOAK_HOST_NAME}/auth/realms/hotsir/protocol/openid-connect/auth",
    "token_uri": "https://${WAU_KEYCLOAK_HOST_NAME}/auth/realms/hotsir/protocol/openid-connect/token",
    "userinfo_uri": "https://${WAU_KEYCLOAK_HOST_NAME}/auth/realms/hotsir/protocol/openid-connect/userinfo",
    "issuer": "https://${WAU_KEYCLOAK_HOST_NAME}/auth/realms/hotsir",
    "redirect_uris": [
      "https://${WAU_HOST_NAME}/oidc_callback"
    ]
  }
}
